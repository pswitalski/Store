import styled from "styled-components";

export const StyledExampleLoginData = styled.div`
    margin: auto 0;
    color: ${props => props.theme.colors.dark};
    font-size: 14px;
    line-height: 22px;
`;

export const StyledUl = styled.ul`
    list-style: none;
    text-align: center;
`;